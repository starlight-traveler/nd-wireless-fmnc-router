#include "timing.h"
#include <chrono>
#include "server.h" // For quill::Logger
#include <cmath>

void start_timing(TimingInfo &timing) {
    // LOG_DEBUG(logger, "  Throughput: {:.2f} bytes/s ({:.2f} bits/s)", bytes_per_second, bits_per_second);
}

void end_timing(TimingInfo &timing)
{
    // LOG_DEBUG(logger, "  Throughput: {:.2f} bytes/s ({:.2f} bits/s)", bytes_per_second, bits_per_second);
}